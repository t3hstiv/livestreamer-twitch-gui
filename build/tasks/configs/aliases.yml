default:
    - "dev"
dev:
    - "build"
    - "concurrent:runbuild"
build:
    - "jshint:tasks"
    - "jshint:app"
    - "clean:tmp"
    - "copy:build"
    - "metadata"
    - "less:dev"
    - "requirejs:dev"
buildrelease:
    - "jshint:tasks"
    - "jshint:app"
    - "test"
    - "clean:tmp"
    - "copy:build"
    - "metadata"
    - "less:release"
    - "requirejs:release"
    - "uglify"
test:
    - "buildtest"
    - "connect:test"
    - "qunit"
buildtest:
    - "jshint:test"
    - "clean:test"
    - "copy:test"
    - "metadata"
    - "requirejs:test"
